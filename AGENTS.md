# AGENTS.md

## 工作规则

- 每次开始新会话或处理本项目任务前，如果根目录存在 `REQUIREMENTS.md`，必须先阅读它；项目需求以该文档和用户最新确认的内容为准。
- 分析、审查、讲解不修改文件。
- 任何工作区改动都要先说明目标、范围和文件，得到用户明确同意后再执行。
- 新功能遵循：需求确认 → 设计方案 → 用户同意 → 实现 → 验证。
- 新模块由用户先提出第一版设计，agent 负责审查唯一性、并发、一致性、删除、权限、性能和边界。
- 设计方案写入 `docs/<模块>.md` 前先获得同意；开始实现前再次明确实现范围并获得同意。
- 只修改用户点名的范围，不顺手重构无关代码。
- 用户要求停止或退回时，立即停止，并撤销本会话由 agent 产生的改动；保留用户原有改动。
- 代码教学以行业规范、可维护性和实际风险为准。用户方案存在问题时，应明确指出原因和替代方案。
- `AGENTS.md` 只记录稳定规则、运行方式、已知坑和模块索引，不记录历史过程、完整设计或临时讨论。

## 项目定位与复杂度控制

- 这是大二学生用于求职的项目，目标是在可理解、可解释和可运行的前提下展示技术能力，不以生产项目的完整严苛程度为目标。
- 优先实现范围明确的 MVP；核心权限、事务边界、数据一致性和已知业务错误必须处理，但不为了追求“绝对生产级”而默认引入复杂字段、重试体系、并发控制、极端参数校验或额外基础设施。
- 设计和实现需要明确区分“当前项目版本”和“生产环境增强方案”。生产级扩展应先说明必要性、复杂度和面试解释成本，再由用户决定是否加入。
- 教学和代码 review 应优先保证用户能够理解并在面试中讲清楚；不要一次堆叠超出当前技术能力范围的抽象、框架机制或容错链路。

## 代码实现

- 优先沿用项目中已经存在的编码方式和结构，不为同一问题引入多种风格。
- 命名应准确表达业务含义，避免无意义的缩写。
- 方法和类保持清晰的职责边界，避免过度抽象和层层封装。
- 重要业务规则、非直观的实现逻辑和关键边界应添加简洁注释。
- 注释解释“为什么这样做”，不重复描述显而易见的代码。
- 涉及并发、一致性、权限、数据删除或外部调用时，应说明关键风险和处理依据。
- 修改代码后进行与风险相匹配的验证，并说明验证范围和结果。

## 文档维护

- 接口路径、请求参数和返回类型以当前代码为准；模块文档应在实现后同步更新。
- `docs/<模块>.md` 记录当前设计、表结构、API、事务边界和实现映射。
- 文档涉及用户可能陌生的框架机制、关键 API 或非直观的并发、事务、一致性逻辑时，补充简短的用途、调用时机和风险说明；显而易见的语法不展开。
- 已实现文档不得保留“待确认”“尚未实现”等过时表述；历史方案移到 `docs/notes/` 或删除。
- 学习笔记只在用户主动要求时写入 `docs/notes/`。
